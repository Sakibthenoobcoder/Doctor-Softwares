{
  "name": "doctor-software",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "make": "electron-packager . doctor-software-app --overwrite --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=doctor-software --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Electron Tutorial App\""
  },
  "author": "Sakib",
  "license": "ISC",
  "devDependencies": {
    "electron": "^14.0.0"
  },
  "dependencies": {
    "browser-window": "^0.4.0"
  }
}
